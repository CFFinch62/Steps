step: is_even
    belongs to: validation
    expects: num
    returns: result
    
    declare:
        remainder as number
    
    do:
        set remainder to num modulo 2
        
        if remainder is equal to 0
            set result to true
        otherwise
            set result to false
        
        return result

