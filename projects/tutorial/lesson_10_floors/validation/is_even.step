step: is_even
    belongs to: validation
    expects: number
    returns: result
    
    declare:
        remainder as number
    
    do:
        set remainder to number % 2
        
        if remainder is equal to 0
            set result to true
        otherwise
            set result to false
        
        return result

