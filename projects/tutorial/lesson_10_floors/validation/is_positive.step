step: is_positive
    belongs to: validation
    expects: number
    returns: result
    
    do:
        if number is greater than 0
            set result to true
        otherwise
            set result to false
        
        return result

