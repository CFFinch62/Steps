step: get_numeric_input
    belongs to: user_interaction
    expects: prompt as text
    returns: value as number

    declare:
        value_text as text
        value as number

    do:
        display prompt
        set value_text to input

        attempt:
            set value to value_text as number
        if unsuccessful:
            set value to 0

        return value
